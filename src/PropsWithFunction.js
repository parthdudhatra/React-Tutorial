// use to props in the react 
//props means we use as parameter of variable let look the example

export function PropsWithFunction(props) {
    return(
        <div>
            <h1>Hello {props.name}</h1>
        </div>  
    )
}